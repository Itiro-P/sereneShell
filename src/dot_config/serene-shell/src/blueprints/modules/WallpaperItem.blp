using Gtk 4.0;
using Adw 1;

template $WallpaperItem : Button {
  styles ["wallpaper-item"]
  overflow: hidden;

  child: Adw.Clamp clamp_h {
    orientation: vertical;

    child: Adw.Clamp clamp_w {
      child: Picture picture {
        content-fit: cover;
      };
    };
  };
}